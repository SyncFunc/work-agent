# Step M8.6 子 agent 渲染接入 + 验收

## 实现方案

- 目标：子 agent 渲染接入（新增 `_SubAgentTuiTransport` 或复用父 `RichLog` 汇入），并完成全量验收
  （`pytest` + 真 TTY 体验对标调研 §2）。
- 改动文件：
  - 新建 `agent/runtime/_subagent_tui_transport.py`（`_SubAgentTuiTransport(TextualTransport)` 或轻量版）：
    子 agent 有独立 `EventStream`，渲染委派给父 `App`（加「▶ subagent: name」前缀汇入主区 `RichLog`），
    复用现有累积 / 裁剪逻辑（与现状 `_SubAgentTransport` 一致）。
  - `agent/tui/app.py`：子 agent 事件前缀渲染；若用 Panel / Group（现有 `SubagentPanelHub` 思路）汇入。
  - `tests/unit/test_tui.py`：补充子 agent 渲染用例。
- 关键接口/算法：
  - 与 `_SubAgentTransport`（旧）行为对齐（渲染委派父传输）；子 agent 有独立 `EventStream`（现状不变）。
- 依赖/环境：M8.1–M8.5 全部完成。

## 验收标准

- [ ] `test_subagent_render`：子 agent 事件经 `_SubAgentTuiTransport` 汇入主区，断言出现「▶ subagent」前缀块。
- [ ] 全量 `pytest -q` 通过（含 `test_tui.py`，旧 `CliRunner` 单测仍绿）。
- [ ] 真 TTY 手动体验清单（§2）逐项核对：流式 Markdown / 工具折叠块 / diff 高亮 / HITL 模态 /
  Ctrl+P / 主题切换 / 鼠标滚动。
- [ ] 不变量：子 agent 独立 `EventStream` 不变；旧 `_SubAgentTransport` 保留供旧 CLI。

## 知识沉淀

> 完成本步后填写：① `_SubAgentTuiTransport` 与 `_SubAgentTransport` 的差异与对齐点；
> ② 子 agent `Renderable` 汇入主区的约定（前缀 / Panel）；
> ③ 真 TTY 验收问题与修复记录（若有）。
