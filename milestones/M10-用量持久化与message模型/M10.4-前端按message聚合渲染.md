# Step M10.4 前端按 message 聚合渲染

## 实现方案

- 目标：前端从「全会话单一内存 `turnMeta`」切换为「按 `message` 挂载的 `turnMeta`」，并应用**子 agent 归集规则**——子 agent 的 `usage` + `duration` 向上归集到根 message（顶层 `ResponseBlock`），子 agent 块不渲染消耗信息。实时与回放走同一条 reducer 路径，重启后可恢复。

- 改动文件：
  - `desktop/src/protocol/types.ts`（`AgentEvent` 加 `message_id` / `parent_message_id`；`EventTypeStr` 加 `'usage'`；`UsagePayload` → `UsageEvent`）
  - `desktop/src/features/chat/useEventReducer.ts`（透传 message 字段 + 新增 `usage` 事件处理 + 归集映射）
  - `desktop/src/renderer/App.tsx`（移除内存版 `turnMeta` / `turnUsageRef` / `turnStartRef` / `estimatedRef` 与 `onMessage('usage')`；可选保留进行中 message 的轻量实时耗时 tick）
  - `desktop/src/features/obs/useObs.ts` + `StatusBar.tsx`（由 `usage` 事件累积会话级 usage，回放即恢复）
  - `desktop/src/features/chat/ResponseToolbar.tsx`（读 `block.turnMeta`，每轮独立明细）
  - `desktop/src/features/chat/MessageList.tsx` / `MessageItem.tsx`（`turnMeta` 从「全会话 prop」改为「每块自带 `turnMeta`」）
  - `desktop/src/features/chat/SubagentBlock` 组件（**不渲染** `turnMeta`）

- 关键接口 / 算法：

  **1. `useEventReducer.ts` 的 `usage` 事件处理（核心）**
  ```ts
  // 维护 messageId -> rootMessageId 映射（子 agent 的 root = 其父的 root）
  const rootOf = (m: string, parent?: string | null): string => {
    if (!parent) return m            // 顶层 message 即根
    rootMap.set(m, parent)           // 直接父；查询时再向上追溯
    return findRoot(parent)
  }
  // USAGE 事件一律累加到 root 顶层 ResponseBlock 的 turnMeta
  case 'usage': {
    const root = resolveRoot(ev.message_id, ev.parent_message_id)
    const block = blocksByMessageId[root]   // 顶层 ResponseBlock
    block.turnMeta = block.turnMeta ?? { prompt_tokens: 0, completion_tokens: 0, total_tokens: 0, duration: 0, estimated: ev.estimated }
    block.turnMeta.total_tokens  += ev.usage.total_tokens  ?? 0
    block.turnMeta.prompt_tokens += ev.usage.prompt_tokens ?? 0
    // ... 其余 token 字段累加；duration 累加
    block.turnMeta.duration += ev.duration
    // 子 agent 块（parent_message_id 非空）不挂 turnMeta
    break
  }
  ```
  - 嵌套子 agent：`resolveRoot` 沿 `rootMap` 向上追溯至根，自然归集到同一根 message。
  - 顶层 message（`parent_message_id` 空）：`turnMeta` 直接挂到该 message 对应的顶层 `ResponseBlock`。

  **2. `App.tsx` 清理**
  - 删除 `useState` 的内存版 `turnMeta` / `turnUsageRef` / `turnStartRef` / `estimatedRef`。
  - 删除 `onMessage('usage', ...)` 监听。
  - 可选：保留「当前进行中 message」的轻量实时耗时 tick（仅展示进行中，不持久化、不进入 reducer）。

  **3. `useObs.ts` + `StatusBar.tsx`**
  - 会话级 usage 改为由 `usage` 事件累积（`turnMeta` 合计后的数值或逐事件累加），回放即恢复，不再依赖内存。

  **4. `ResponseToolbar.tsx`**
  - 读取 `block.turnMeta`（每轮独立明细），展示「tokens / 耗时」。

  **5. `SubagentBlock`**
  - **不渲染** `turnMeta`（任何 token / 耗时信息都不出现在子 agent 卡片底部）。

- 依赖 / 环境：依赖 M10.3 已删除 `MsgType.USAGE` 并完成类型投影；本步只消费 `event` 内的 `USAGE` 事件。

## 验收标准

- [x] 前端门禁：`npm run typecheck`=0、`npm run test`（`vitest run`）=0、`npm run build`=0；Python 门禁：`ruff check`=0、`ruff format --check`=0、`basedpyright`=0、全量 `pytest`=0。
- [x] 行为：顶层 `ResponseBlock` 的 `turnMeta` 由 `useEventReducer` 累积父会话 + 子 agent（子会话段提取）的 `USAGE` 事件；`ResponseToolbar` 从 `block.turnMeta` 读取并渲染 tokens/耗时。子 agent 块 `SubagentCard` 不渲染消耗。
- [x] 行为：回放路径（replay）与实时路径走同一 `buildChatModel` reducer；`USAGE` 事件在回放流中同样被归集，重启后历史用量可恢复。
- [x] 不变量：`App.tsx` 无内存版 `turnMeta`/`turnUsageRef`/`turnStartRef`/`estimatedRef`；`MessageList` 不再传 `turnMeta` prop；`ResponseGroup` 直接从 `block.turnMeta` 读取。实时与回放路径一致。

## 知识沉淀

### 与文档方案的实现差异（重要）
- 文档方案设计了完整的 `resolveRoot(message_id, parent_message_id)` 链条归集算法，但实测前端 `AgentEvent` 的 `message_id` 字段仅在 `USAGE` 事件上是有效值（非 USAGE 事件的 `message_id` 已由后端 `current_message_id` 自动打标，但前端 `AgentEvent` 不确保全体事件携带）。因此**实际未实现 `resolveRoot` 消息树追溯**，改用更简洁的**队列机制**。
- 队列机制原理：`buildChatModel` 维护一个 `currentUsage` 累积器（`TurnMeta` 结构）+ `usageQueue` 队列。父会话 `processParent('usage')` 和子会话段提取的 USAGE 事件均累积到 `currentUsage`。`USER` 事件触发 `flush`（推入 `usageQueue`，重置 `currentUsage`）。主循环结束后 `flush` 剩余用量。最后按顺序 `usageQueue[i]` 分配给第 i 个 `ResponseBlock.turnMeta`。该方案**无需消息 ID 跟踪**，仅依赖事件顺序（USAGE 始终在本轮消息的 FINAL 之后 / 下一个 USER 之前），经测试覆盖全部多轮 + 子 agent + 回放场景。

### 关键接口 / 约定
- `ResponseBlock.turnMeta?: TurnMeta | null`：新字段（`TurnMeta = { duration: number, usage: UsageSummary }`，`UsageSummary` 从 `MessageItem.tsx` 迁移到 `useEventReducer.ts` 统一导出）。
- `MessageList` 移除 `turnMeta` prop：不再从 `App.tsx` 传递内存 `turnMeta`，而是从 `ResponseBlock` 自带的字段读取。
- `App.tsx` 删除：`useState<TurnMeta>`、`turnStartRef`、`turnUsageRef`、`estimatedRef`、`emptyUsage()`、`addUsage()`、`finishTurn`（简化仅 `setRunning(false)`）、`onEvent('usage')` 分支、`<MessageList turnMeta={turnMeta}>` prop、session 切换 `setTurnMeta(null)`。
- 子 agent 用量提取：在主循环 `else` 分支（subsession 段）收集 `segEvents` 后，扫描其中的 `'usage'` 事件累积到 `currentUsage`，再构建 `SubagentBlock`。子 agent 段内的 USAGE 事件在 `reduceSubEvents` 中仍被 `default: break` 忽略。

### 踩坑
- **`TurnMeta`/`UsageSummary` 迁移造成的 imports 连锁反应**：原来在 `MessageItem.tsx` 导出并给 `ResponseToolbar.tsx` 使用。迁移到 `useEventReducer.ts` 后，`ResponseToolbar` 和 `MessageList` 的 import 路径必须从 `'./MessageItem'` 改为 `'./useEventReducer'`，否则 `tsc` 报 `TS2459: Module declares TurnMeta locally, but it is not exported` 与 `TS6196: UsageSummary declared but never used`。
- 由于 `MessageList` 不再接收 `turnMeta` prop，连带着 `renderBlock` 的 `opts.turnMeta` 和 `ResponseGroup` 的 `turnMeta` prop 也一并移除；`ResponseGroup` 直接从 `block.turnMeta` 读取。这要求 `ResponseGroup` 内部正确读取 `block.blocks` 的最后一个 text index 并向下传递 `turnMeta`。
- **`UsageSummary` 的双向依赖**：`useEventReducer.ts` 导出 `UsageSummary`，`MessageItem.tsx` 之前也导出同名的 `UsageSummary`。M10.4 迁移后 `MessageItem` 不再导出，若其他文件（如 `App.tsx`）仍 `import type { UsageSummary } from './MessageItem'` 会 `TS6196: UsageSummary declared but never used`（`App.tsx` 已不再需要）。
