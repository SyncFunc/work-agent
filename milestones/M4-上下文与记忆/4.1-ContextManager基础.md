# Step M4.1 ContextManager 基础

## 实现方案

**目标**：创建 `agent/context/` 包，实现 `ContextManager` 核心类——持有 `conv` 投影、估算 token 分类明细、记录压缩历史、维护 Compact Boundary 位置。同时新增 `Settings` 上下文窗口配置。

**改动文件**：
- `agent/context/__init__.py`（新建）
- `agent/context/manager.py`（新建，ContextManager + ContextUsage + CompactRecord）
- `agent/context/compactors/__init__.py`（新建，Compactor 协议接口）
- `agent/config/settings.py`（新增 ContextConfig 嵌套模型）

**关键接口**：

```python
# agent/context/manager.py

@dataclass
class ContextUsage:
    """上下文占用分类明细。"""
    system_fixed: int = 0     # System Prompt 静态段（可缓存）
    system_dynamic: int = 0   # System Prompt 动态段（日期/Git 状态等）
    tools: int = 0            # Tools 列表
    messages: int = 0         # 对话历史（user/assistant/tool_result）
    total: int = 0            # 总和
    available: int = 0        # 有效窗口 − total（剩余可用）
    used_pct: float = 0.0     # total / 有效窗口

@dataclass
class CompactRecord:
    """一次压缩的记录。"""
    ts: float                  # 时间戳
    method: str                # "microcompact" / "auto_compact" / "session_memory"
    before_tokens: int
    after_tokens: int

class ContextManager:
    def __init__(
        self,
        context_window: int = 200_000,
        max_output_tokens: int = 20_000,
        compact_buffer: int = 13_000,
        *,
        system_fixed_tokens: int = 3_000,
        system_dynamic_tokens: int = 0,
        tools_tokens: int = 15_000,
    ):
        self.conv: list[Message] = []
        self.context_window = context_window
        self.max_output_tokens = max_output_tokens
        self.compact_buffer = compact_buffer
        self.effective_window = context_window - min(max_output_tokens, 20_000)
        self.compact_threshold = self.effective_window - compact_buffer
        self.compact_boundary: int = 0  # 索引边界：之前的内容已被压缩
        self.history: list[CompactRecord] = []
        self._system_fixed = system_fixed_tokens
        self._system_dynamic = system_dynamic_tokens
        self._tools = tools_tokens

    def estimate_usage(self) -> ContextUsage:
        """估算当前上下文占用分类明细。"""
        msg_tokens = self._estimate_conv_tokens()
        fixed = self._system_fixed
        dynamic = self._system_dynamic
        tools = self._tools
        total = fixed + dynamic + tools + msg_tokens
        available = max(0, self.effective_window - total)
        used_pct = total / self.effective_window if self.effective_window > 0 else 0
        return ContextUsage(fixed, dynamic, tools, msg_tokens, total, available, used_pct)

    def should_compact(self) -> bool:
        """是否需要触发压缩？"""
        usage = self.estimate_usage()
        return usage.used_pct >= 0.93  # 约 ~187K/180K（200K 窗口）

    def mark_boundary(self) -> None:
        """记录当前 conv 索引为压缩边界。"""
        self.compact_boundary = len(self.conv)

    def _estimate_conv_tokens(self) -> int:
        """估算 conv 中 boundary 之后的 token 数。"""
        text = ""
        for msg in self.conv[self.compact_boundary:]:
            text += (msg.content or "") + (msg.name or "")
            if msg.tool_calls:
                for tc in msg.tool_calls:
                    text += tc.name + json.dumps(tc.arguments, sort_keys=True)
        return _estimate_tokens(text)

    def set_conv(self, conv: list[Message]) -> None:
        """设置当前上下文投影（每次 loop.run 前调用）。"""
        self.conv = conv

    def get_active_messages(self) -> list[Message]:
        """获取 boundary 之后的有效消息（压缩前原文 + 压缩后消息）。"""
        return list(self.conv)


# agent/context/compactors/__init__.py
class Compactor(Protocol):
    """压缩器协议。"""
    async def compact(
        self,
        conv: list[Message],
        boundary: int,
    ) -> list[Message]:
        """把 boundary 前历史压缩为摘要，返回替换后的消息列表。"""
        ...
```

```python
# agent/config/settings.py 新增
class ContextConfig(BaseModel):
    context_window: int = 200000
    max_output_tokens: int = 20000
    compact_buffer: int = 13000
    microcompact_keep_recent: int = 5
    microcompact_enabled: bool = True
    auto_compact_enabled: bool = True
    session_memory_enabled: bool = True
    session_memory_dir: str = ".agent/sessions"
    agents_md_path: str = "AGENTS.md"
    agents_md_enabled: bool = True

class Settings(BaseSettings):
    # ... 已有字段 ...
    context: ContextConfig = ContextConfig()
```

**依赖/环境**：
- 复用 `agent/runtime/terminal_transport.py` 的 `_estimate_tokens`（抽取到 `agent/context/` 共享，或直接导入）。
- `Message` 类（`agent/core/model.py`）—— 直接引用，不改动。
- 无新增外部依赖。

## 验收标准

- [ ] `agent/context/manager.py` 编译无错误，`from agent.context import ContextManager, ContextUsage` 可导入。
- [ ] `ContextManager.estimate_usage()` 返回分类明细，`total = fixed + dynamic + tools + messages` 恒成立。
- [ ] `ContextManager.should_compact()` 在空历史时返回 False，在大量消息时返回 True。
- [ ] `ContextManager.mark_boundary()` 后 `compact_boundary` 等于当前 `len(conv)`。
- [ ] `Settings.context` 可读，`settings.context.context_window == 200000`。
- [ ] 测试：`tests/test_context.py` 新建，≥10 用例覆盖计量 / 边界 / 阈值 / 历史记录。

## 知识沉淀

> 完成本步后填写。关键内容：
> - ContextManager 接口签名与参数默认值
> - 有效窗口公式：`effective_window = context_window − min(max_output_tokens, 20000)`
> - 阈值公式：`compact_threshold = effective_window − compact_buffer`（默认 13K buffer）
> - `should_compact` 触发条件：`used_pct >= 93%`（≈ `effective_window − compact_buffer`）
> - `_estimate_tokens` 从 `terminal_transport.py` 抽取到共享位置，避免循环导入
> - `Compactor` 协议接口定义在 `agent/context/compactors/__init__.py`
