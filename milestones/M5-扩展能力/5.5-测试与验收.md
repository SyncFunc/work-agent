# Step M5.5 测试与验收

## 实现方案

**目标**：用 `FakeModel` / `RecordingModel` 驱动 M5 全链路（不联网），断言 Skill 与 Subagent 的核心不变量；补齐 `tests/test_skills.py` 与 `tests/test_subagent.py`，并在 `tests/test_loop.py` / `tests/test_cli.py` 追加集成用例。

**新增/改动测试文件**：
- `tests/test_skills.py`（新建，≥10 用例）：发现/解析/触发目录/参数替换/自动启用判定/CLI 列表。
- `tests/test_subagent.py`（新建，≥12 用例）：发现/上下文隔离/工具白名单/模型降级/深度限制/Trace 父子/NullTransport/内置类型/spawn 集成。
- `tests/test_loop.py`（追加，≥6 用例）：`use_skill` 注入、`spawn_subagent` 摘要回填、白名单、深度、Trace 父子、不变量（主流不含子事件）。
- `tests/test_cli.py`（追加，≥3 用例）：`/skills` `/agents` 展示、显式 `/skill` 加载、未知名提示。

**关键测试策略（对齐项目「LLM 一律可 Mock」）**：

```python
# 子 agent 上下文隔离（核心不变量）
def test_subagent_context_isolated():
    base = FakeModel([Decision(text="child final")])
    spawner = SubagentSpawner(settings, max_depth=5)
    spec = BUILTIN_GENERAL
    result = asyncio.run(spawner.spawn(spec, "do X", base_model=base))
    # 子 messages 从 [] 起，父看不到子内部
    assert result.text == "child final"
    # 父 conv 不污染：spawn 不修改调用方 messages
```

```python
# Skill 正文按需加载（省 token 不变量）
def test_skill_body_not_loaded_at_discover():
    loader = SkillLoader(tmp_project_root)
    specs = loader.discover()
    catalog = loader.catalog_prompt()
    assert "SKILL.md 正文长文本" not in catalog   # 正文未进入 catalog
    spec = loader.get("demo")
    assert "SKILL.md 正文长文本" in spec.body()     # 仅按需读取
```

```python
# 工具白名单
def test_explore_subagent_rejects_write():
    base = FakeModel([Decision(text="ok")])
    spawner = SubagentSpawner(settings)
    result = asyncio.run(spawner.spawn(BUILTIN_EXPLORE, "search", base_model=base))
    # Explore 白名单只含 read/grep/glob/bash；write 不在 registry
    # 用 RecordingModel 断言子 loop 收到的 tools 不含 write
```

```python
# Trace 父子（不 reset 到根）
def test_subagent_span_parented():
    tracer = Tracer()
    base = FakeModel([Decision(text="x")])
    spawner = SubagentSpawner(settings, tracer=tracer)
    parent = tracer.span("agent.run")
    with parent:
        result = asyncio.run(spawner.spawn(BUILTIN_GENERAL, "t",
                              depth=0, parent_span=parent, base_model=base))
    # 子 model.act span.parent_id == parent.id
```

**全量验收命令**：

```bash
pytest -q                 # 全量需全绿，含 M1–M4 零回归
pytest tests/test_skills.py tests/test_subagent.py -q
pytest --cov=agent
```

## 验收标准

- [ ] `tests/test_skills.py` ≥10 用例全绿。
- [ ] `tests/test_subagent.py` ≥12 用例全绿。
- [ ] `tests/test_loop.py` 集成用例（use_skill / spawn_subagent / 白名单 / 深度 / Trace 父子）全绿。
- [ ] `tests/test_cli.py` 追加 `/skills` `/agents` 用例全绿。
- [ ] 全量 `pytest -q` 全绿，M1–M4 零回归。
- [ ] 不变量全部通过：① Skill 正文不在 discover 阶段进上下文；② 子 agent `messages` 与父隔离；③ 主 `EventStream` 不含子内部事件；④ 深度≥5 抛 `RecursionError`；⑤ 子 span 正确 parent 到父。
- [ ] 覆盖率：`agent/skills/` 与 `agent/subagent.py` 行覆盖 ≥80%。

## 知识沉淀

**测试总数与结果**（M5.4/M5.5 落地后全量）：

- `tests/test_skills.py`：**30 用例**（远超 ≥10）——覆盖发现（项目/用户级、同名覆盖、忽略无 SKILL.md 目录、缺失 get 返回 None）、frontmatter 解析（全字段、snake/kebab 兼容、YAML 错误降级）、触发目录不变量（catalog 只含 name+trigger、discover 后 `_body_cache is None`）、正文按需加载与缓存、参数替换（`$ARGUMENTS` / `$N` / `$ARGUMENTS[N]` / 命名 / `${SKILL_DIR}` / `\$` 转义 / 无 token 追加）、自动启用判定（disable/默认/paths glob/ `**` glob）、trigger_text 截断。
- `tests/test_subagent.py`：**24 用例**（远超 ≥12）——覆盖发现与内置类型、解析 agent 文件、上下文隔离与 fork、工具白名单（丢弃 disallowed / explore 拒 write）、模型降级（继承/覆盖）、深度限制 `RecursionError`、Trace 父子（含并行安全）、`_SubAgentTransport`（委托/屏蔽 HITL/独立 stream）。
- `tests/test_loop.py`：M5.3 已追加 **10 用例**（use_skill 注入 / spawn 返回摘要 / 深度超限 / explore 免审批 / explore 拒 write / 父 stream 不被子事件污染 / agents_catalog 列出类型等）。
- `tests/test_cli.py`：M5.4 已追加 **3 用例**（`/agents` / `/skills`+`/skill` / 注入 messages）。

**全量验收**：`pytest -q` → **314 passed**（M1–M4 零回归）。

**不变量全部验证通过**（散落各测试）：
1. Skill 正文不在 discover 阶段进上下文（`test_invariants_no_body_in_discover`）。
2. 子 agent `messages` 与父隔离（`test_spawn_returns_summary_and_isolated` / `test_no_fork_excludes_parent_messages`）。
3. 主 `EventStream` 不含子内部事件（`test_parent_eventstream_not_polluted_by_subagent` / `test_parent_eventstream_clean_after_spawn`）。
4. 深度≥`max_depth` 抛 `RecursionError`（`test_depth_limit_raises` / `test_spawn_subagent_depth_limit_returns_error`）。
5. 子 span 正确 parent 到父（`test_trace_parent_links_child_span` / `test_subagent_span_parented`）。

**覆盖率**：`agent/skills/` 与 `agent/subagent.py` 行覆盖目标 ≥80%；**注意**：本环境未安装 `pytest-cov`（运行 `--cov` 报错 `No module named 'pytest_cov'`），覆盖率未实测。需先 `pip install pytest-cov` 或 `pip install -e ".[dev]"`，再跑 `pytest --cov=agent.skills --cov=agent.subagent` 复核。

**跨步骤踩坑汇总**：
- M5.3：`AgentLoop.__init__` 必须显式初始化 `_agent_span=None` / `_transport=None`（直接调 `_tool_spawn_subagent` 测试路径时不初始化会 `AttributeError`）。
- M5.4：`AgentTransport` 新增方法用 `...` 占位（协议方法），loop 不调用故既有测试 fake transport 不需补实现；`Session` 必须**持有** `skill_loader`/`subagent_spawner`（M5.3 仅局部传参，导致 CLI 无法直接查询——已在 M5.4 改为 `self` 属性）。
- M5.4：chat REPL 命令匹配用 `cmd.lower()` 但提取 skill 名用原 `task`（保留大小写）。
- 全链路测试统一用 `FakeModel`/`RecordingModel`，不触真实 LLM/工具副作用；spawn 路径给子 agent 独立 `FakeModel`（避免父子共享队列冲突）。

**结论**：M5 全部步骤（5.1–5.5）落地，Skill（双轨加载/触发目录）与 Subagent（内置 explore/plan/general-purpose + 自定义、白名单/权限/深度/隔离/Trace 父子）完整接入主循环与 CLI，全量测试绿。
